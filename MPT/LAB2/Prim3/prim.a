START:	JMP LOAD ;переход к программе
		ORG 2100H;директива размещени€ программы с адреса 2100
LOAD:	MOV R0,#0X41 ;загрузка в R0 указател€ данных
		MOV @R0,#0FFH ;запись в пам€ть числа 
		INC R0 ;инкремент указател€
		MOV @R0,#021H ;запись в пам€ть числа
		INC R0 ;инкремент указател€
		MOV @R0,#0FFH ;запись в пам€ть числа
		
		MOV R2, #0
		
		
		MOV R1, #4
		CLR C ;сброс переноса
		MOV A,#0 ;сброс аккумул€тора
	
		MOV A, #0 ;????? ????????????
		LOOP: 
		ADD A,@R0 ;???????? ?????????
		MOV B,#5 ;???????? ?????????
		MUL AB 
		ADDC A,R2
		MOV @R0,A ;?????? ???????? ????? ??????????
		
		DEC R0 ;?????????? ??????
		MOV A,B ;????????? ???????? ????? ??????????
		;???????????? ? ???????????
		MOV R2,A ;??????????????? ????????????
		;?????????? ????? ????????????
		DJNZ R1,LOOP 	
;LOOP:	ADD A, @R0 
;		MOV B, #0X5	
;		MUL AB 
;		MOV @R1,A 
;		DEC R1
;		MOV A,B 
;		XCH A,@R1
;		DEC R1
;		DEC R0
;		DJNZ R2,LOOP 
;		
;		MOV R1, #0X55
;		MOV R0, #0X54
;		MOV A, @R1
;		ADDC A, @R0
;		MOV @R1, A
;		
;		DEC R1
;		MOV A, @R1
;		DEC R1
;		ADDC A, @R1
;		MOV @R1, A

		END